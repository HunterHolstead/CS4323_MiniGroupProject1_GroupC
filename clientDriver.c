//  Group C client driver
#include "MessagePassing.h"
#include "clientHelper.h"

// function to run the client side of the program
int clientDriver() {
    while(1) {
        menu(); // display the menu to the user
    }

    return 0;
}